# Dependencies (reinstalled fresh in the image)
node_modules

# Version control
.git
.github
.gitignore

# Env files (inject via K8s secrets/configmaps at runtime instead)
.env
.env.example

# OS/editor junk
.DS_Store

# Docker-related (no need inside the image)
docker/dev/Dockerfile
docker/prod/Dockerfile
docker/dev/docker-compose.yaml
docker/prod/docker-compose.yaml
.dockerignore